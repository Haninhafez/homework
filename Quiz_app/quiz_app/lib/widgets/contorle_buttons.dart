import 'package:flutter/material.dart';

class ContorleButtons extends StatelessWidget {
  const ContorleButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(children: [Container(child: Text())]);
  }
}
