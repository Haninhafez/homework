import 'package:flutter/material.dart';

class ListChoiceAnswer extends StatelessWidget {
  const ListChoiceAnswer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
