import 'dart:ui';

import 'package:flutter/material.dart';

class ColorsApp {
  static final BoxShadow mybox = BoxShadow();
  static final myGradiant = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [Color(0xFF2B0063), Color(0xFFA838FF), Color(0xFF2B0063)],
  );
}
